(defvar
  tap_timeout 200
  hold_timeout 200
  long_hold_timeout 300
)

(include def/base.kbd)
(include def/lt_hrm.kbd)
(include def/symbol_num.kbd)
(include def/nav.kbd)
(include def/alias.kbd)

(defcfg
  process-unmapped-keys yes
  windows-altgr cancel-lctl-press
)

;; vim: set ft=lisp:
